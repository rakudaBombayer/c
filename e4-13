#include <stdio.h>

int main(void)
{
	int num;

	printf("整数を入力せよ : ");
	fflush(0);
	scanf("%d", &num);

	int sum = 0;

	for (int i = 1; i <= num; i++) {
		sum += i;
	}

	printf("nの値 : %d\n", num);
	printf("1から%dまでの総和は%dです。", num ,sum);

	return 0;

}

